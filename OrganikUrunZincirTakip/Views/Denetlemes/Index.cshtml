@model IEnumerable<OrganikUrunZincirTakip.Models.Denetleme>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SertifikaID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DenetlemeAcıklama)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.User.Username)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SertifikaID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DenetlemeAcıklama)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.User.Username)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DenetlemeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DenetlemeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DenetlemeID })
        </td>
    </tr>
}

</table>
